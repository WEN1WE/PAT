#include <iostream>
using namespace std;

const int MAX = 1020;
int graph[MAX][MAX];
int dist[MAX];
int visited[MAX];




int main() {
    int N, M, K, D;
    scanf("%d %d %d %d", &N, &M, &K, &D);
    
    for (int i = 0; i < K; i++) {
        
    }
    
    
}
